[user]
    name = Chris Reeves
    email = chris@thisissoon.com
	signingkey = 0D7AD5E5B70B5254
[core]
    editor = vim
    filemode = false
[alias]
    co = checkout
    br = branch
    cm = commit -S
    st = status
    ap = add -p
    ammend = commit --amend -S
    unstage = reset HEAD --
    last = log -1 HEAD
    lg = log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an %ae>%Creset' --abbrev-commit
    up = pull
	lt = describe --tags --abbrev=0
[color]
    ui = auto
[color "diff"]
    meta = blue bold
    frag = magenta bold
    old = red bold
    new = green bold
[color "status"]
    added = green
    changed = yellow
    untracked = red
[hub]
    protocol = https
[filter "lfs"]
	clean = git-lfs clean %f
	smudge = git-lfs smudge %f
	required = true
[rebase]
	autoStash = true
[pull]
	rebase = true
[url "git@gitlab.com:"]
    insteadOf = https://gitlab.com/
